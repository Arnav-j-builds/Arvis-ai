langchain==0.3.26
langchain-community==0.3.27
langchain-core==0.3.70
langchain-ollama==0.3.5
langchain-openai==0.3.28
ollama==0.5.1
openai==1.97.0
python-dotenv==1.1.1
pyttsx3==2.99
pytz==2025.2
SpeechRecognition==3.14.3
PyAudio>=0.2.11

# Additional tool dependencies
mss==10.0.0
duckduckgo-search==8.1.1
pytesseract==0.3.13

# -------------------------------------------------------------
# New modules (vision / communication / routines)
# -------------------------------------------------------------
Pillow>=10.0.0
requests>=2.31.0

# Vision - OCR engines
# Choose ONE of the following:
#   Tesseract (lighter) - also requires the OS-level binary, see README.
#   EasyOCR (more accurate, downloads models on first use).
easyocr>=1.7.0

# Vision - webcam capture
opencv-python>=4.8.0

# Vision - active window capture on Windows
pywin32>=306; sys_platform == "win32"

# Vision - active window capture on macOS
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# -------------------------------------------------------------
# Hand-mouse control (webcam gesture -> mouse events)
# All three are optional at import time - the tool degrades to a
# friendly error when any of them is missing.
# -------------------------------------------------------------
mediapipe>=0.10.0
pyautogui>=0.9.54

# -------------------------------------------------------------
# Web UI (run via: python web_server.py)
# -------------------------------------------------------------
flask>=3.1.0
flask-cors>=6.0.0
flask-socketio>=5.6.0
eventlet>=0.40.0
psutil>=7.0.0
